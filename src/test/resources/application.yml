server:
  port: 8888
cors:
  originPatterns: http://localhost:8080
security:
  jwt:
    token:
      secret-key: 73573
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
      show-sql: true
